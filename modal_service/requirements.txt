# Since coqui-tts 0.27.4, PyTorch is NOT bundled — install torch/torchaudio
# explicitly. openai-whisper is the open-source model package, not the API client.
# transformers pinned <5.0: coqui-tts 0.27.4 requires transformers>=4.57 but
# its tortoise/XTTS layers still import isin_mps_friendly from
# transformers.pytorch_utils, which transformers 5.x removed.
# "codec" extra pulls in torchcodec, required for audio IO since PyTorch 2.9.
coqui-tts[codec]==0.27.4
torch>=2.2.0
torchaudio>=2.2.0
transformers>=4.57.0,<5.0.0
openai-whisper>=20231117
modal>=0.73.0
fastapi>=0.115.0
